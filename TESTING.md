# 整合測試指南 — 步驟五

## 📋 測試準備

### 1️⃣ 安裝依賴

在專案根目錄執行：

```bash
# 建立虛擬環境（如果還未建立）
python -m venv .venv

# 激活虛擬環境
# Windows:
.venv\Scripts\activate
# macOS/Linux:
source .venv/bin/activate

# 安裝依賴
pip install -r requirements.txt
```

### 2️⃣ 初始化測試資料

```bash
# 執行初始化指令稿
python init_test_data.py
```

**輸出預期：**
```
🗑️  清空資料庫...
📊 建立資料表...
👤 建立測試用戶...
   ✅ 帳號: admin | 密碼: password | 本金: NT$ 10000
⚙️  建立用戶設定...
   ✅ 每日目標: NT$ 1000 | 停損: NT$ 500
🎰 建立測試房間...
...
✅ 測試資料初始化完成！

📋 測試帳號信息:
   帳號: admin
   密碼: password
   本金: NT$ 10000
```

### 3️⃣ 啟動伺服器

```bash
python app.py
```

**預期輸出：**
```
✅ 資料庫初始化完成
📊 開發伺服器啟動: http://127.0.0.1:5000
```

---

## 🧪 功能測試清單

### ✅ Test 1: 訪問首頁（未登入）

**步驟：**
1. 開啟瀏覽器，訪問 `http://127.0.0.1:5000/`
2. 確認是否自動重導向至 `/login`

**預期結果：**
- ✅ 自動跳轉至登入頁面
- ✅ 頁面顯示登入表單（帳號、密碼輸入框）
- ✅ 顯示測試帳號提示

---

### ✅ Test 2: 測試登入 — 錯誤密碼

**步驟：**
1. 帳號輸入框：輸入 `admin`
2. 密碼輸入框：輸入 `wrongpassword`
3. 點擊「登入」按鈕

**預期結果：**
- ✅ 顯示紅色警告訊息：「帳號或密碼錯誤」
- ✅ 留在登入頁面
- ✅ 表單值被清空

---

### ✅ Test 3: 測試登入 — 成功

**步驟：**
1. 帳號輸入框：輸入 `admin`
2. 密碼輸入框：輸入 `password`
3. 點擊「登入」按鈕

**預期結果：**
- ✅ 顯示綠色成功訊息：「登入成功！」
- ✅ 3 秒後自動重導向至主控台 (`/dashboard`)
- ✅ 頂部導覽列顯示用戶名 `admin` 和登出選項

---

### ✅ Test 4: 主控台頁面 — 信息顯示

**步驟：**
1. 確認已登入，在主控台頁面 (`/dashboard`)
2. 檢查頁面元素

**預期結果：**
- ✅ 左側顯示帳戶概覽
  - 使用者：admin
  - 初始本金：NT$ 10,000.00
  - 目前餘額：NT$ 10,000.00
  - 目前虧損：NT$ 0.00 (0%)
- ✅ 中央顯示風控設定表單
  - 每日出金目標：1000
  - 停損金額：500
  - 自動操作開關
- ✅ 右側顯示推薦房間表格（5 個房間）
- ✅ 顯示最近警告區塊

---

### ✅ Test 5: 風控設定 — 修改並保存

**步驟：**
1. 在主控台的風控設定區塊
2. 修改「每日出金目標」為 `2000`
3. 修改「停損金額」為 `1000`
4. 勾選「啟用自動操作」
5. 點擊「💾 保存設定」按鈕

**預期結果：**
- ✅ 按鈕顯示載入狀態（可選）
- ✅ 顯示成功訊息：「設定保存成功！」
- ✅ 2 秒後頁面自動重新載入
- ✅ 重新載入後，表單值反映新的設定

**驗證資料庫：**
```bash
# 在 Python shell 中檢查
from app import create_app
from app.models import UserSettings
app = create_app()
with app.app_context():
    settings = UserSettings.query.filter_by(user_id=1).first()
    print(f"Daily Target: {settings.daily_target}")  # 應該是 2000
    print(f"Stop Loss: {settings.stop_loss_amount}")  # 應該是 1000
    print(f"Auto Play: {settings.auto_play_enabled}")  # 應該是 True
```

---

### ✅ Test 6: 實時監控 — 房間更新

**步驟：**
1. 停留在主控台頁面
2. 等待 5-10 秒
3. 觀察推薦房間表格

**預期結果：**
- ✅ 每 5 秒房間表格應該更新一次（觀察時間戳記）
- ✅ 房間資訊（勝率、賠率）有可能變化
- ✅ 最後更新時間顯示當前時間

**檢查瀏覽器控制台：**
- 按 F12 開啟開發者工具
- 進入 Network 選項卡
- 應該看到每 5 秒有一個對 `/api/monitor/status` 的 GET 請求
- 回應應該是 200 OK，並返回 JSON 格式的監控數據

---

### ✅ Test 7: 操作日誌頁面

**步驟：**
1. 點擊頂部導覽列的「操作日誌」
2. 進入 `/logs` 頁面

**預期結果：**
- ✅ 左側顯示操作日誌表格
  - 包含「時間」、「操作類型」、「金額」、「狀態」欄位
  - 至少顯示之前的登入記錄
- ✅ 右側顯示警告記錄表格
  - 包含「時間」、「警告類型」、「風險等級」、「狀態」欄位
  - 顯示初始化時建立的警告記錄

---

### ✅ Test 8: 返回主控台

**步驟：**
1. 在日誌頁面底部，點擊「← 返回主控台」按鈕

**預期結果：**
- ✅ 返回主控台頁面 (`/dashboard`)

---

### ✅ Test 9: 登出

**步驟：**
1. 點擊頂部導覽列右上角的用戶名下拉選單
2. 點擊「登出」選項

**預期結果：**
- ✅ 顯示藍色提示訊息：「已登出系統」
- ✅ 重導向至登入頁面 (`/login`)
- ✅ Session 已清除（無法直接訪問 `/dashboard`）

**驗證 Session 已清除：**
- 嘗試直接訪問 `http://127.0.0.1:5000/dashboard`
- 應該自動重導向至 `/login` 並顯示警告訊息

---

### ✅ Test 10: 健康檢查 API

**步驟：**
1. 訪問 `http://127.0.0.1:5000/health`

**預期結果：**
- ✅ 返回 JSON 回應：`{"status": "ok", "database": "connected"}`
- ✅ HTTP 狀態碼：200

---

## 🐛 常見錯誤排查

### 錯誤 1：模組導入失敗

```
ModuleNotFoundError: No module named 'flask'
```

**解決方案：**
```bash
# 確保虛擬環境已激活
pip install -r requirements.txt
```

### 錯誤 2：資料庫文件權限問題

```
PermissionError: [Errno 13] Permission denied: '...\instance\database.db'
```

**解決方案：**
- 刪除 `instance/` 目錄
- 重新執行 `python app.py`（自動建立）

### 錯誤 3：端口被佔用

```
OSError: [Errno 48] Address already in use
```

**解決方案：**
```bash
# 尋找佔用端口 5000 的進程並終止，或改用其他端口
python app.py  # 預設 5000
# 修改 app.py 中的 port 參數或使用環境變數
FLASK_ENV=development flask run --port 5001
```

### 錯誤 4：Jinja2 模板找不到

```
TemplateNotFound: login.html
```

**解決方案：**
- 確認 `app/templates/` 目錄及所有 HTML 文件都存在
- 檢查文件名是否完全一致（區分大小寫）

---

## 📊 測試結果匯總

如果所有 10 項測試都通過，則表示：

✅ **路由邏輯正常運作**
- 登入/登出功能完整
- 頁面導航正確
- API 端點可訪問

✅ **模板渲染正確**
- Jinja2 模板語法正確
- 變數替換有效
- 表單處理正常

✅ **資料庫操作正確**
- Model 增刪查改功能正常
- 日誌記錄成功
- Session 管理有效

✅ **前端互動正常**
- AJAX 輪詢成功
- 表單提交有效
- 頁面更新流暢

---

## 🎉 完成

如果所有測試都通過，恭喜！您已成功完成整個 Web 應用開發流程：

1. ✅ 步驟一：初始化專案
2. ✅ 步驟二：實作 Model
3. ✅ 步驟三：實作路由
4. ✅ 步驟四：實作模板
5. ✅ 步驟五：整合測試

**下一步：**
- 根據結果修復任何發現的 Bug
- 進行負載測試與壓力測試
- 部署至生產環境
- 設定持續集成 (CI/CD)

---

**文件版本：** v1.0  
**最後更新：** 2026-04-29  
**作者：** GitHub Copilot
